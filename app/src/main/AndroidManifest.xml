<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcommerceNassim"
        tools:targetApi="31">

        <!-- activité de lancement -->
        <activity
            android:name=".customer.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- autres activités  -->
        <activity android:name=".MainActivity" />
        <activity android:name=".customer.RegisterActivity" />
        <activity android:name=".customer.ProductActivity" />
        <activity android:name=".customer.ProductDetailActivity" />
        <activity android:name=".customer.CategoryActivity" />
        <activity android:name=".customer.UserActivity" />
        <activity android:name=".admin.ProductAdminActivity" />
        <activity android:name=".admin.UpdateProductAdminActivity" />

    </application>

</manifest>
